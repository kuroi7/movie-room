.container
  %h1= @movie_info['title']
  %table.table
    %p
      %strong 上映日：
      = @movie_info['release_date']
    %p
      %strong 上映時間：
      = @movie_info['runtime']
      分
    %p
      %strong ジャンル：
      - @movie_info['genres'].each do |genre|
        - if genre == @movie_info['genres'].last
          %span= genre['name']
        - else
          %span
            = genre['name']
            、
    %p
      %strong ストーリ：
      = @movie_info["overview"]
      %img(src = "https://image.tmdb.org/t/p/original#{@movie_info["poster_path"]}" alt = "poster" class="img-fluid")
.new_btm
  = link_to new_post_path(id:@movie_info["id"],title:@movie_info["title"],poster_path:@movie_info["poster_path"]), class: "btn btn-primary" do
    レビューする