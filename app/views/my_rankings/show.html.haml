.postTitle
  = @MyRanking.title
.postDate
  = @MyRanking.created_at
.postAuthor
.postManage
  -# - if user_signed_in? && current_user.id == @post.user_id
  -#   =link_to edit_post_path(@MyRanking.id), class: "postManage__edit" do
  -#     編集
  -#   = link_to post_path(@MyRanking.id), method: :delete, class: "postManage__delete" do
  -#     削除
.postText
  = simple_format @MyRanking.content 
