.message_main
  .message_main-header
    .message_main-header__box
      .main-header__box__leftbox
        .message_main-header__box__leftbox--group_name
          = @group.name
        .message_main-header__box__leftbox--user_name
          - @group.users.each do |user|
            =user.name
      =link_to edit_group_path(@group.id) do
        .message_main-header__box__edit-btn
          edit
  .messages
    .message
      = render @messages

  .message_form
    = form_for [@group, @message] do |f|
      .message_form__box
        .message_form__box__message-area
          = f.text_field :content, class: 'message_form__box__message-area--text', placeholder: 'type a message'
          .message_form__mask
            = f.label :image, class: 'message_form__box__message-area__pic' do
              = icon('fas', 'image')
              = f.file_field :image, class: 'message_form__box__message-area__pic--icon'
        = f.submit 'Send', class: 'message_form__box__send-btn'